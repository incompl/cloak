## How to set up

* Do `npm install` in
  * ./server
  * ./client
  * cloak/src/server
* Copy `cloak.js` into `./client/lib`
* Run `./run.sh`
* Run `node ./client/test-server.js` or a server of your choice

